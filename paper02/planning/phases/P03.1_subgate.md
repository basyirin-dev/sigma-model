# Phase 03.1 — Sub-Gate Protocol (Conditional; RPF v2.0)

| Field | Value |
|-------|-------|
| Tasks / Subtasks | **3 / 2** |
| Duration | 1 day |
| Compute | ≤ 1 GPU hr total |
| Git tag on close | `p03.1-resolved` |
| Trigger | P03 gate verdict = **INCONCLUSIVE** (primary metric within the pre-specified ambiguity band: $k \in [10, 15)$ with ≥ 1 separation inequality violated by < 2 SE) |
| Status | ⬜ Template — instantiated only if triggered |

> This phase is **pre-specified and bounded**. It is NOT open-ended iteration. If P03.1 does not resolve to PASS, the project returns to P02.

---

## Tasks

| # | Task | Subtasks | Notes |
|---|------|----------|-------|
| T1 | Confirm trigger: verify `planning/gate-result.md` verdict = INCONCLUSIVE and the ambiguity band condition holds. | S1: Trigger check logged | Re-evaluate pre-registered decision rule (`preregistration.md` §5) |
| T2 | Execute ≤ 3 additional gate runs, each with a **different seed from `meta/seeds.yaml`** (never reusing P03 seeds), same protocol (`experiments/configs/gate_protocol.yaml`), manifests per CC.1.4. | S2: Per-run manifest + numerical sanity checks | Compute budget ≤ 1 GPU hr total |
| T3 | Binary verdict: if ≥ 2/3 runs PASS → overall **PASS** (unlock P04); if ≥ 2/3 FAIL → **FAIL** (return to P02); if 1 PASS / 1 FAIL / 1 ambiguous → **FAIL** (conservative). Update `gate-result.md` verdict + ledger `decide-at-P03` rows. | — | No further iteration allowed |

## Human Gates

- `[HUMAN-GATE]` PI reads the updated `gate-result.md` and issues PROCEED / RE-GATE / ABORT (same protocol as P03; SLA 48h, fallback RE-GATE).
## Machine-Checkable Exit Criteria

- [ ] Trigger condition documented (INCONCLUSIVE band).
- [ ] 1–3 runs executed with distinct seeds, ≤ 1 GPU hr total.
- [ ] Binary verdict issued per the ≥2/3 rule.
- [ ] `gate-result.md` verdict updated; ledger `decide-at-P03` rows resolved.
- [ ] `[HUMAN-GATE]` directive recorded in `decisions/human-gates/P03_gate_directive.md`.

## Phase-Transition Checklist

- [ ] Git tag `p03.1-resolved` (or `p03-gate-passed` / `p03-gate-failed` after resolution)
- [ ] Commit: `[P03.1][Gate][INIT] Sub-gate resolution: <verdict>`
